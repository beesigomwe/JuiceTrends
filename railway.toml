[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build && npm run db:auto-migrate"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
